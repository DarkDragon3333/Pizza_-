<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_home">

    <fragment
        android:id="@+id/navigation_home"
        android:name="com.example.pizza.Home.HomeFragment"
        android:label="@string/title_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_navigation_home_to_navigation_dashboard"
            app:destination="@id/navigation_dashboard" />
        <action
            android:id="@+id/action_navigation_home_to_navigation_dashboard2"
            app:destination="@id/navigation_dashboard" />
        <action
            android:id="@+id/action_navigation_home_to_navigation_notifications"
            app:destination="@id/navigation_notifications" />
        <action
            android:id="@+id/action_navigation_home_to_pizza_settings"
            app:destination="@id/pizza_settings" />
    </fragment>

    <fragment
        android:id="@+id/navigation_dashboard"
        android:name="com.example.pizza.Basket.Basket"
        android:label="@string/title_dashboard"
        tools:layout="@layout/basket" >

    </fragment>

    <fragment
        android:id="@+id/navigation_notifications"
        android:name="com.example.pizza.About_program.About_program"
        android:label="@string/title_notifications"
        tools:layout="@layout/fragment_notifications" >

    </fragment>
    <fragment
        android:id="@+id/pizza_settings"
        android:name="com.example.pizza.Home.Pizza_settings"
        android:label="fragment_pizza_settings"
        tools:layout="@layout/fragment_pizza_settings" >
        <action
            android:id="@+id/action_pizza_settings_to_navigation_home"
            app:destination="@id/navigation_home" />
    </fragment>
</navigation>